// Soal 2

const mtk = 80
const bahasaIndonesia = 90
const bahasaInggris = 89
const ipa = 69

let nilaiSiswa = [...[mtk], ...[bahasaIndonesia], ...[bahasaInggris], ...[ipa]];
let cekData = (nilaiSiswa.length) ? nilaiSiswa.every(n => n !== null  && typeof(n) == 'number' ) : false;

let jumlahNilai = 0

    if (cekData == true) {
        for (let i = 0; i < nilaiSiswa.length; i++) {
        jumlahNilai = jumlahNilai + nilaiSiswa[i];
        var rataRata = Math.ceil((jumlahNilai/nilaiSiswa.length));
        
        } console.log('Rata-rata: ' + rataRata);
    } else {
        console.log('Data ada yang kosong!')
    }    
    
    if (rataRata >= 90) {
    console.log ('Grade: A')
    } else if (rataRata >= 80 && rataRata <= 89) {
        console.log ('Grade: B')
    } else if (rataRata >= 70 && rataRata <= 79) {
        console.log ('Grade: C')
    } else if (rataRata >= 60 && rataRata <= 69) {
        console.log ('Grade: D')
    } else if (rataRata >= 0 && rataRata <= 59) {
        console.log ('Grade: E')
    } else {
        console.log ('Cek data!')
    }
